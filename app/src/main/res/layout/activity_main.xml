<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="10dp"
    android:orientation="vertical">

    <!--  Button to trigger Floating Widget Service -->
    <Button
        android:id="@+id/create_floating_widget"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:onClick="llamarSiguienteTurno"
        android:text="@string/llamar_turno"
        android:textColor="@android:color/white"
        android:textSize="14sp" />
    <Button
        android:id="@+id/create_floating_widget2"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:onClick="finalizarTurno"
        android:text="@string/finalizar_turno"
        android:textColor="@android:color/white"
        android:textSize="14sp" />
    <Button
        android:id="@+id/create_floating_widget3"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:onClick="createFloatingWidget"
        android:text="@string/create_floating_widget"
        android:textColor="@android:color/white"
        android:textSize="14sp" />
</LinearLayout>

